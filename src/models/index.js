//Models
const User = require('./User');

//  In this index file we also can define some associations, like 
//  User.hasOne(...) or .hasMany()
//  In my opinion that can improve readebles

module.exports = {
  User,
};
